function setup() {
  createCanvas(400, 400);
  background(500);
  
  // Set the text size
  textSize(32);
  
  // Set the text color
  fill(50);
  
  // Display the text at coordinates (50, 200)
  text('', 50, 200);
}